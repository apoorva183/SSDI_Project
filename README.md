# NinerMatch

Student connection platform with resume parsing.

## Setup

```bash
pip install -r requirements.txt
cp .env.example .env
# Edit .env with MongoDB URI
python app.py
```

Open http://localhost:5000